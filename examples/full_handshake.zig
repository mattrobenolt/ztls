//! TLS 1.3 client handshake driven through the public ClientHandshake API.
//!
//! This runs the real driver — init → start → processRecord loop — against the
//! RFC 8448 §3 server records (ServerHello, ChangeCipherSpec, and the encrypted
//! EncryptedExtensions/Certificate/CertificateVerify/Finished flight). Because
//! the server's records are AEAD-protected under keys derived from §3's
//! transcript, the client must feed the exact §3 ClientHello bytes — so this
//! example exercises the handshake driver, not client_hello.encode.
//!
//! After the handshake completes, rx/tx carry application traffic keys; the
//! finale round-trips a message through them to show they are live.
const std = @import("std");
const print = std.debug.print;

const txtar = @import("txtar");
const ztls = @import("ztls");

// RFC 8448 §3 client ephemeral X25519 keypair. We replay the fixed §3
// ClientHello via injectClientHello, so the public key here is unused, but
// init takes the whole keypair.
const client_keypair: ztls.x25519.KeyPair = .{
    .secret_key = .init(.{
        0x49, 0xaf, 0x42, 0xba, 0x7f, 0x79, 0x94, 0x85,
        0x2d, 0x71, 0x3e, 0xf2, 0x78, 0x4b, 0xcb, 0xca,
        0xa7, 0x91, 0x1d, 0xe2, 0x6a, 0xdc, 0x56, 0x42,
        0xcb, 0x63, 0x45, 0x40, 0xe7, 0xea, 0x50, 0x05,
    }),
    .public_key = .init(.{
        0x99, 0x38, 0x1d, 0xe5, 0x60, 0xe4, 0xbd, 0x43,
        0xd2, 0x3d, 0x8e, 0x43, 0x5a, 0x7d, 0xba, 0xfe,
        0xb3, 0xc0, 0x6e, 0x51, 0xc1, 0x3c, 0xae, 0x4d,
        0x54, 0x13, 0x69, 0x1e, 0x52, 0x9a, 0xaf, 0x2c,
    }),
};

// RFC 8448 §3 ClientHello handshake message (4-byte header + body).
const client_hello = [_]u8{
    ,
};

// RFC 8448 §3 ServerHello, wrapped as a plaintext handshake record
// (5-byte record header + 90-byte ServerHello).
const server_hello_record = [_]u8{ 0x16, 0x03, 0x03, 0x00, 0x5a } ++ [_]u8{
    0x02, 0x00, 0x00, 0x56, 0x03, 0x03, 0xa6, 0xaf,
    0x06, 0xa4, 0x12, 0x18, 0x60, 0xdc, 0x5e, 0x6e,
    0x60, 0x24, 0x9c, 0xd3, 0x4c, 0x95, 0x93, 0x0c,
    0x8a, 0xc5, 0xcb, 0x14, 0x34, 0xda, 0xc1, 0x55,
    0x77, 0x2e, 0xd3, 0xe2, 0x69, 0x28, 0x00, 0x13,
    0x01, 0x00, 0x00, 0x2e, 0x00, 0x33, 0x00, 0x24,
    0x00, 0x1d, 0x00, 0x20, 0xc9, 0x82, 0x88, 0x76,
    0x11, 0x20, 0x95, 0xfe, 0x66, 0x76, 0x2b, 0xdb,
    0xf7, 0xc6, 0x72, 0xe1, 0x56, 0xd6, 0xcc, 0x25,
    0x3b, 0x83, 0x3d, 0xf1, 0xdd, 0x69, 0xb1, 0xb0,
    0x4e, 0x75, 0x1f, 0x0f, 0x00, 0x2b, 0x00, 0x02,
    0x03, 0x04,
};

// Middlebox-compatibility ChangeCipherSpec record (RFC 8446 §D.4).
const ccs_record = [_]u8{ 0x14, 0x03, 0x03, 0x00, 0x01, 0x01 };

// RFC 8448 §3 fixtures, base64 inside a txtar archive (decoded at startup).
const rfc8448_archive = @embedFile("test_fixtures/rfc8448.txtar");

// Decode a base64 archive entry into `out`, returning the decoded slice.
fn fixture(alloc: std.mem.Allocator, name: []const u8, out: []u8) ![]u8 {
    var archive = try txtar.parse(alloc, rfc8448_archive);
    defer archive.deinit(alloc);
    for (archive.files) |f| {
        if (!std.mem.eql(u8, f.name, name)) continue;
        const b64 = std.mem.trimEnd(u8, f.data, "\n");
        const n = try std.base64.standard.Decoder.calcSizeForSlice(b64);
        try std.base64.standard.Decoder.decode(out[0..n], b64);
        return out[0..n];
    }
    return error.FixtureNotFound;
}

pub fn main() !void {
    print("=== TLS 1.3 client handshake (RFC 8448 §3) ===\n\n", .{});

    var fixture_buf: [8192]u8 = undefined;
    var fba: std.heap.FixedBufferAllocator = .init(&fixture_buf);
    var flight_buf: [1024]u8 = undefined;
    const server_flight_record =
        try fixture(fba.allocator(), "server_flight_record.b64", &flight_buf);

    var hs: ztls.ClientHandshake = .init(client_keypair);
    // We replay the fixed §3 ClientHello (so the transcript matches the trace),
    // so inject it rather than encoding a fresh one via start().
    hs.injectClientHello(&client_hello);
    print("[client] ClientHello sent ({} bytes)          → state={s}\n", .{
        client_hello.len, @tagName(hs.state),
    });

    var out: ztls.ClientHandshake.OutBuffer = .empty;

    // Each server record goes through the one inbound entry point, handleRecord.
    var sh = server_hello_record;
    _ = try hs.handleRecord(&sh, out.fullSlice());
    print("[server] ServerHello                          → state={s}\n", .{@tagName(hs.state)});

    var ccs = ccs_record;
    _ = try hs.handleRecord(&ccs, out.fullSlice());
    print("[server] ChangeCipherSpec (discarded)         → state={s}\n", .{@tagName(hs.state)});

    const ev = try hs.handleRecord(server_flight_record, out.fullSlice());
    print("[server] EncryptedExtensions/Cert/CV/Finished → state={s}\n", .{@tagName(hs.state)});
    print("[client] Finished sent ({} wire bytes)\n", .{ev.write.len});
    hs.completeWrite(); // acknowledge the Finished was sent

    if (!hs.isConnected()) return error.HandshakeIncomplete;
    print("\n=== handshake complete — application keys installed ===\n\n", .{});

    // Application data round-trip with the negotiated keys. Capture a mirror of
    // our send layer to stand in for the peer's read side.
    var peer = hs.tx;

    const message = "Hello from ztls!";
    var app_out: ztls.ClientHandshake.OutBuffer = .empty;
    const record = try hs.sendApplicationData(message, app_out.fullSlice());
    app_out.resize(@intCast(record.len));
    print("[client] encrypted {} bytes                    → {} wire bytes\n", .{
        message.len, record.len,
    });

    const dec = try peer.decrypt(app_out.slice());
    print("[peer]   decrypted: \"{s}\"\n", .{dec.content});
}
