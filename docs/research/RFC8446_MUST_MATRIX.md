# RFC 8446 MUST coverage matrix

This matrix is the working claims-to-evidence map for TLS 1.3 normative
requirements. It is deliberately conservative: a requirement is not marked as
proved unless there is named test evidence, clear structural evidence in a
single code path, or an explicit out-of-scope decision tied to a GitHub issue.

Status terms:

| Status | Meaning |
|---|---|
| `PROVEN` | Covered by named tests, external conformance evidence, or a small auditable implementation invariant. |
| `PARTIAL` | Some evidence exists, but either the test is indirect or the requirement is only partly enforced. |
| `GAP` | In current supported surface but missing implementation, missing direct evidence, or both. |
| `OUT-OF-SCOPE` | Not in the current ztls supported surface; tracked by the issue named in the row. |
| `CALLER` | Required behavior belongs to the Sans-I/O caller/application boundary, not the core TLS engine. |
| `N/A` | Requirement is for a role ztls does not implement, a legacy TLS mode ztls forbids, or middlebox behavior. |

The current supported surface is the one named in `PRODUCTION_READINESS.md`:
TLS 1.3 full handshake over X25519, the three TLS 1.3 cipher suites ztls
implements, certificate-authenticated server flight with explicit client
verification gates, application data, alerts, `close_notify`, post-handshake
KeyUpdate, and parse/discard handling for NewSessionTicket. HRR, PSK/resumption,
0-RTT, client authentication, P-256/P-384/PQ groups, exporters, and TLS 1.2
fallback are not supported today.

Rows below group tightly related RFC sentences only when they have the same
implementation path, evidence, and disposition. A grouped row is still a
normative claim: splitting it would duplicate the same evidence and status. The
issue asks for MUST/MUST NOT/SHALL plus security-relevant SHOULDs; SHOULD-level
rows are kept in the same matrix but the row text names them as `should` rather
than promoting them to MUSTs.

## Matrix

| ID | RFC | Requirement | Status | Evidence / disposition | Notes |
|---|---|---|---|---|---|
| R002-001 | §2 | Server's ephemeral Diffie-Hellman share must correspond to a client-offered share. | PARTIAL | `src/ServerHandshake.zig` tests `acceptClientHello: emits ServerHello and installs handshake keys`, `in-memory authenticated client-server handshake suite matrix`; `src/client_hello.zig` test `parse: no shared group is rejected (HRR not implemented)`. | Holds by construction for X25519-only today, but no direct mixed-group or unsolicited-server-group test. |
| R002-002 | §2 | Application Data must not be sent before Finished, except 0-RTT. | PROVEN | `src/ClientHandshake.zig` tests `handleRecord: application_data before ServerHello is rejected`, `handleRecord: encrypted application data during server flight is rejected`; `src/ServerHandshake.zig` test `handleRecord: rejects application_data before connected`. | 0-RTT is out of scope. |
| R002-003 | §2.1, §4.1.1 | If no common cryptographic parameters can be negotiated, the server must abort with `handshake_failure` or `insufficient_security`; if a compatible group lacks a key share, server should use HRR. | PARTIAL | `src/ServerHandshake.zig` test `acceptClientHello: rejects unsupported suite`; `src/client_hello.zig` test `parse: no shared group is rejected (HRR not implemented)`. | ztls aborts unsupported groups because HRR is not implemented (#1). Alert-description mapping is not exhaustively proven. |
| R002-004 | §2.2, §4.2.9, §4.2.11 | PSK/resumption requirements: PSK hash provisioning, PSK modes, selected identity, binders, binder order, compatible cipher suite hash, and ticket/SNI binding. | OUT-OF-SCOPE | PSK/session resumption is tracked by #2. | NewSessionTicket is parsed and discarded only; no ticket store or PSK binder path exists. |
| R002-005 | §2.3, §4.2.10, §4.5, §8 | 0-RTT requirements: replay-safe caller policy, early-data extension validation, EndOfEarlyData, anti-replay storage/freshness checks, and no automatic replay. | OUT-OF-SCOPE | 0-RTT is tracked by #3. | ztls does not send or accept early data. |
| R003-001 | §3, Appendix B | TLS presentation-language encoding: network-byte-order integers, length-prefixed vectors, enum wire values, and declared field order. | PROVEN | `src/wire.zig` tests `Writer.append: u16 big-endian`, `Writer.append: u24 big-endian`, `Writer.append: enum`, `Reader.read: u16 big-endian`, `Reader.read: enum`, `Reader.readSlice: zero-copy`; protocol encode/parse tests in `client_hello.zig`, `server_hello.zig`, `certificate.zig`, `NewSessionTicket.zig`. | Appendix B mostly restates wire formats exercised by specific parser/encoder tests below. Variant/select behavior is covered only where implemented message formats use it. |
| R004-001 | §4 | Handshake messages must be sent in the RFC order; unexpected handshake messages must abort with `unexpected_message`. | PARTIAL | `src/ClientHandshake.zig` tests `processFlight: rejects Finished before EncryptedExtensions`, `processFlight: rejects wrong CertificateVerify signature`, `handleRecord: malformed ServerHello is rejected`; `src/ServerHandshake.zig` tests `processClientFinished: verifies Finished and installs app keys`, `handleRecord: rejects application_data before connected`. | Client flight ordering has strong tests. Server unexpected handshake-message coverage is thinner. |
| R004-002 | §4.1.1 | If server selects a PSK, it must select a key-establishment mode from the client's `psk_key_exchange_modes`. | OUT-OF-SCOPE | #2. | PSK not implemented. |
| R004-003 | §4.1.1, §4.1.4 | If selected group lacks a compatible key share, server must respond with HRR; client must process HRR transcript, extensions, cipher-suite continuity, and ClientHello2 restrictions. | OUT-OF-SCOPE | HRR state-machine retry path is tracked by #1. | ztls has HRR parser/transcript helpers, but the live retry state machine is not implemented. |
| R004-004 | §4.1.2 | ClientHello is the first client TLS handshake message. | PROVEN | `src/ClientHandshake.zig` tests `handleRecord: drives RFC 8448 §3 handshake to connected`, `clientFinished: RFC 8448 §3 emits Finished and upgrades to app keys`; `src/client_hello.zig` encode tests. | Full known-answer handshake begins with ztls ClientHello. |
| R004-005 | §4.1.2 | TLS 1.3 endpoint receiving ClientHello at any other time must terminate with an `unexpected_message` alert; TLS 1.2 renegotiation behavior is forbidden. | GAP | No direct post-handshake second-ClientHello test. | Renegotiation is not supported; the matrix still needs evidence that it is rejected in connected state and mapped to the required alert. |
| R004-006 | §4.1.2 | ClientHello `legacy_version` must be 0x0303. | PROVEN | `src/client_hello.zig` test `encode: handshake type and legacy version`. | Initial record-layer legacy version is separate and covered under record rows. |
| R004-007 | §4.1.2 | ClientHello compatibility-mode `legacy_session_id` must be non-empty/32 bytes; otherwise zero-length; generated value should be unpredictable. | PARTIAL | `src/server_hello.zig` test `encode: echoes session id`; full handshake tests require echo to work. | ztls emits a zero-length ClientHello `legacy_session_id` today; there is no direct ClientHello encode assertion for that policy or any compatibility-mode generation path. |
| R004-008 | §4.1.2 | Server must ignore unrecognized cipher suites and process recognized alternatives. | GAP | `src/ServerHandshake.zig` test `acceptClientHello: rejects unsupported suite` proves only the all-unsupported case. | Mixed known/unknown-list behavior is not tested. |
| R004-009 | §4.1.2 | ClientHello `legacy_compression_methods` must contain exactly one zero byte; server must abort with `illegal_parameter` on any other value. | PROVEN | Client encode path emits the compatibility value; `src/client_hello.zig` test `parse: rejects malformed compression methods`. | End-to-end alert description mapping remains covered by R006-005. |
| R004-010 | §4.1.2, §9.3 | Servers must ignore unrecognized ClientHello extensions. | PROVEN | `src/client_hello.zig` test `parse: ignores unknown extension`. | Applies to server parser for ClientHello. |
| R004-011 | §4.1.2, §4.2.1 | TLS 1.3 server must negotiate from `supported_versions`, ignore `legacy_version`, ignore unknown versions, and abort nonconforming ClientHello with `missing_extension` when required extensions are absent. | PARTIAL | `src/client_hello.zig` tests `parse: encoded ClientHello`, `parse: rejects malformed ClientHello`; full server tests parse ztls ClientHello. | Missing-`supported_versions`, unknown-version, and ignore-legacy-version behavior need direct tests. |
| R004-012 | §4.1.2 | TLS 1.2-or-earlier compatibility parsing of ClientHello extensions after `legacy_compression_methods`. | N/A | ztls is TLS 1.3 only. | No TLS 1.2 fallback. |
| R004-013 | §4.1.3, §4.2.1 | ServerHello `legacy_version` must be 0x0303 and TLS 1.3 selection must be in `supported_versions`. | PROVEN | `src/server_hello.zig` tests `parse: RFC 8448 §3 ServerHello`, `parse: rejects invalid legacy version`, `parseHelloRetryRequest: rejects invalid legacy version`, `parse: missing extensions`, and `parse: unsupported TLS version`. | |
| R004-014 | §4.1.3 | ServerHello random must be generated independently; downgrade sentinels are required only when negotiating older TLS. | PARTIAL | Server uses caller/random-backed generation in handshake tests. | TLS 1.2 downgrade-sentinel emission is N/A, but random independence is not directly testable in unit form. |
| R004-015 | §4.1.3 | Client must abort with `illegal_parameter` if `legacy_session_id_echo` differs from ClientHello session id. | PROVEN | `src/server_hello.zig` test `parse: rejects mismatched session id echo`; `src/ClientHandshake.zig` test `processServerHello: rejects mismatched session id echo`. | End-to-end alert description mapping remains covered by R006-005. |
| R004-016 | §4.1.3 | Client must abort with `illegal_parameter` if ServerHello selects a cipher suite not offered by the client. | PARTIAL | `src/server_hello.zig` test `parse: rejects unknown cipher suite`; `src/ClientHandshake.zig` RFC 8448 tests. | Unknown suite is rejected. A known-but-unoffered suite and alert mapping should get a direct client state-machine test. |
| R004-017 | §4.1.3 | ServerHello `legacy_compression_method` must be zero. | PROVEN | `src/server_hello.zig` tests `parse: rejects non-zero compression method` and `parseHelloRetryRequest: rejects non-zero compression method`. | End-to-end alert description mapping remains covered by R006-005. |
| R004-018 | §4.1.3, §4.2 | ServerHello must contain only cryptographic-context extensions requested by the client and must include `supported_versions` plus `key_share`/PSK as applicable; unsolicited extension responses require `unsupported_extension`. | PARTIAL | `src/server_hello.zig` tests `parse: missing extensions`, `parse: rejects duplicate supported_versions`, `parse: rejects duplicate key_share`. | No test rejects forbidden/extra or unsolicited ServerHello extensions with the required alert. |
| R004-019a | §4.1.3 | Client must examine ServerHello.random first to distinguish the HRR sentinel. | PROVEN | `src/server_hello.zig` test `parse: rejects HelloRetryRequest`; HRR parser tests `parseHelloRetryRequest: RFC 8448 §5`. | Sentinel detection exists and does not fall through as ordinary ServerHello. |
| R004-019b | §4.1.3, §4.1.4 | Client must process an HRR sentinel as HelloRetryRequest. | OUT-OF-SCOPE | HRR state-machine retry path is tracked by #1. | ztls currently rejects HRR in the live client. |
| R004-020 | §4.1.3 | TLS 1.3 client receiving TLS 1.2-or-below ServerHello must check downgrade sentinels and abort on match. | GAP | `docs/research/NEGATIVE_SPACE.md` names this gap. | Defense-in-depth even though ztls does not negotiate TLS 1.2. |
| R004-021 | §4.2 | Endpoint must not send extension responses unless peer requested them, and receiver must abort unsolicited responses with `unsupported_extension` except HRR cookie. | PARTIAL | `src/encrypted_extensions.zig` tests `parse: rejects unsolicited ALPN`, `parse: rejects unoffered ALPN`. | ALPN in EncryptedExtensions is covered; general unsolicited-extension handling across ServerHello, EncryptedExtensions, and CertificateEntry extensions is not. |
| R004-022 | §4.2 | Recognized extension in a message where it is not specified must abort with `illegal_parameter`. | PARTIAL | `src/encrypted_extensions.zig` tests `parse: rejects forbidden supported_versions extension` and `parse: rejects forbidden key_share extension`; duplicate/placement checks exist for some blocks. | EncryptedExtensions is covered for TLS 1.3 recognized extensions in ztls's supported surface; ServerHello and CertificateEntry extension-placement checks remain thinner. |
| R004-023 | §4.2 | `pre_shared_key` must be last in ClientHello; no extension block may contain duplicate extension types. | PARTIAL | Duplicate tests: `client_hello.zig` `parse: rejects duplicate supported_groups`; `server_hello.zig` `parse: rejects duplicate supported_versions`, `parse: rejects duplicate key_share`; `encrypted_extensions.zig` `parse: rejects duplicate ALPN extension`; `NewSessionTicket.zig` `parse: rejects duplicate early_data extension`; `certificate_request.zig` `parse rejects duplicate signature_algorithms`. | General duplicate rule is covered for implemented recognized extensions, not every possible extension block/type. PSK ordering is #2. |
| R004-024 | §4.2.1, §9.2 | ClientHello must send `supported_versions` with TLS 1.3 when prepared to negotiate TLS 1.3. | PARTIAL | `src/client_hello.zig` test `parse: encoded ClientHello`; server handshake tests consume it. | Add direct encode assertion for extension presence/value 0x0304. |
| R004-025 | §4.2.1 | Client must check ServerHello `supported_versions` before processing rest; if absent, unsupported, unoffered, or prior to TLS 1.3, abort with `illegal_parameter`. | PROVEN | `src/server_hello.zig` tests `parse: missing extensions`, `parse: unsupported TLS version`; full client RFC 8448 tests. | Offered-vs-unoffered version distinction is degenerate because only TLS 1.3 is supported. |
| R004-026 | §4.2.2 | Cookie extension behavior for HRR/stateless servers. | OUT-OF-SCOPE | HRR retry path tracked by #1. | HRR parser has cookie tests, but live cookie processing is not implemented. |
| R004-027 | §4.2.3, §9.2 | Certificate-authenticating clients must send `signature_algorithms`; server must abort certificate auth if it is absent; `signature_algorithms_cert` is MTI. | PARTIAL | ClientHello encode includes signature algorithms; `certificate_request.zig` has `parse rejects missing signature_algorithms`. | Server missing-`signature_algorithms` ClientHello rejection and distinct `signature_algorithms_cert` support need proof/implementation. |
| R004-028 | §4.2.3, §4.4.2.2, §4.4.3 | Certificate keys and CertificateVerify signature algorithms must be compatible with the certificate key and offered signature schemes. | PARTIAL | `src/certificate.zig` tests `verifySignature: valid ECDSA P-256 signature`, `verifySignature: wrong transcript hash`, `certificate_policy.verifyServerAuth: unsupported signature algorithm is rejected`. | Compatibility is partly enforced; cross-checking CertificateVerify scheme against the client's actually offered schemes and mismatch tests are gaps. |
| R004-029 | §4.2.3 | RSA-PSS salt/parameter/OID rules and RSASSA-PSS requirements. | PARTIAL | `src/certificate.zig` verification uses OpenSSL EVP/PSS for supported RSA-PSS schemes. | No direct PSS parameter/salt/OID tests; OpenSSL handles primitive verification. |
| R004-030 | §4.2.3, §4.4.2.4, §9.1 | SHA-1/MD5/SHA-224/DSA must not be offered or negotiated in TLS 1.3; MD5 certificates must be rejected; SHA-1 certificates are not recommended except trust-anchor carve-out. | PARTIAL | `src/certificate.zig` test `certificate_policy.verifyServerAuth: unsupported signature algorithm is rejected`; `signature_scheme.zig` exposes only modern TLS 1.3 schemes. | Add explicit absence/rejection tests for MD5/SHA-1/DSA/SHA-224 and verify MTI `rsa_pkcs1_sha256` certificate-signature support. |
| R004-031 | §4.2.4, §4.2.5, §4.2.6, §4.3.2, §4.4.2.3, §4.6.2 | CertificateAuthorities, OID filters, CertificateRequest, client Certificate/CertificateVerify, and post-handshake authentication requirements. | OUT-OF-SCOPE | Full client certificate authentication is tracked by #4. | Parser slices exist, but live client-auth state machines/policy are not implemented. |
| R004-032 | §4.2.7 | Client must not act on `supported_groups` before successful handshake completion. | PROVEN | `src/encrypted_extensions.zig` test `parse: ignores supported_groups extension`; RFC 8448 full-flight client tests include server `supported_groups` in EncryptedExtensions and complete without using it. | ztls ignores the extension in the current supported path. |
| R004-033 | §4.2.8 | Client KeyShareEntry values must correspond to `supported_groups`, appear in the same order, be independently generated, and not duplicate or include unlisted groups. | PARTIAL | `src/client_hello.zig` tests `encode: key_share contains public key`, `parse: no shared group is rejected (HRR not implemented)`; `src/x25519.zig` tests key generation vectors. | Single X25519 share makes most rules structural; duplicate/out-of-list client offers need server parser tests. |
| R004-034 | §4.2.8 | ServerHello must contain exactly one KeyShareEntry for selected (EC)DHE, in the same group as the client share, and not outside client's supported groups. | PARTIAL | Full handshake tests over X25519; `src/server_hello.zig` tests `parse: rejects duplicate key_share`. | Needs direct test for known-but-unoffered or mismatched group. |
| R004-035 | §4.2.8.1, §4.2.8.2, §7.4 | Peers must validate DH/ECDHE public keys; X25519 all-zero shared secret must abort. | PROVEN | `src/x25519.zig` test `sharedSecret: RFC 7748 X25519 vector`; `src/wycheproof.zig` tests `Wycheproof boundary: X25519 identity element is rejected`, `Wycheproof boundary: X25519 small-order public key is rejected`. | P-256/P-384 validation is #6/#22. |
| R004-036 | §4.3.1 | Server must send EncryptedExtensions immediately after ServerHello, encrypted under server handshake traffic keys. | PROVEN | `src/ServerHandshake.zig` tests `sendAnonymousFlightForTest: client decrypts EncryptedExtensions and Finished`, `sendAuthenticatedFlight: client decrypts authenticated server flight`, `sendAuthenticatedFlight: client processes CertificateVerify and Finished`. | Flight construction is single-path. |
| R004-037 | §4.3.1 | Client must check EncryptedExtensions for forbidden extensions and abort with `illegal_parameter`. | PROVEN | `src/encrypted_extensions.zig` tests `parse: rejects forbidden supported_versions extension` and `parse: rejects forbidden key_share extension`; parser rejects the TLS 1.3 recognized extensions assigned to ClientHello, ServerHello, HRR, or CertificateRequest. | End-to-end alert description mapping remains covered by R006-005. |
| R004-038 | §4.4.1 | Transcript hash must include handshake message headers and not record headers. | PROVEN | `src/ClientHandshake.zig` tests `transcript hash: RFC 8448 §3 ClientHello || ServerHello`; `src/transcript.zig` tests `messageHashSynthetic: SHA-256 header`, `messageHashSynthetic: RFC 8448 §5 ClientHello1 SHA-256`. | HRR message_hash use is #1. |
| R004-039 | §4.4.2 | Server must send Certificate when using certificate authentication. | PROVEN | `src/ServerHandshake.zig` tests `sendAuthenticatedFlight: client decrypts authenticated server flight`, `sendAuthenticatedFlight: client processes CertificateVerify and Finished`; `src/interop.zig` OpenSSL interop tests. | PSK-only is out of scope. |
| R004-040 | §4.4.2 | Server Certificate `certificate_request_context` must be zero length; server certificate_list must be non-empty. | PARTIAL | `src/certificate.zig` encode path emits empty context; parser has `error.EmptyCertificateList`; certificate parse tests cover valid chains. | Add direct client rejection tests for non-empty server context and empty certificate_list/alert mapping. |
| R004-041 | §4.4.2 | Sender certificate must be first in `certificate_list`; certificate extensions must correspond to peer-offered extensions. | GAP | Chain validation tests: `parse: validates leaf-intermediate-root chain`, `parse: rejects chain with missing intermediate`. | Leaf-first is assumed by parser/policy; no explicit swapped-chain test. CertificateEntry extensions are skipped. |
| R004-042 | §4.4.2.1 | OCSP/SCT CertificateEntry extension formats must be valid; server must not send `status_request_v2`. | GAP | CertificateEntry extensions are skipped. | Needed if ztls claims these extensions; otherwise document as ignored/unsupported policy. |
| R004-043 | §4.4.2.2, RFC 5280 refs | Server certificate must be X.509v3 unless another type is negotiated; digitalSignature KeyUsage, serverAuth EKU, and supported RFC 5280 name constraints must be enforced. | PARTIAL | `src/certificate.zig` tests `parse: extracts public key from ECDSA P-256 certificate`, `certificate_policy.verifyServerAuth: KeyUsage without digitalSignature is rejected`, `certificate_policy.verifyServerAuth: EKU without serverAuth is rejected`, `parse: rejects missing trust anchor by default`, trust-chain tests, name-constraints chain tests; `cryptox/Certificate.zig` name-constraints unit tests. | DirectoryName constraints and full bettertls harness breadth remain outside this local evidence. |
| R004-044 | §4.4.2.2 | Server certificate chains must use client-advertised signature algorithms when the server can provide such a chain; fallback algorithms have SHA-1 restrictions; unacceptable chains require a certificate-related alert, defaulting to `unsupported_certificate`. | GAP | `certificate_policy.verifyServerAuth: unsupported signature algorithm is rejected`. | Intermediate signature algorithms are not cross-checked against the client's advertised list, and alert mapping is not proven. |
| R004-045a | §4.4.2.4 | Client must abort with `decode_error` if server sends an empty Certificate. | PARTIAL | `src/certificate.zig` returns `error.EmptyCertificateList`; client error-path tests reject malformed/corrupt flights. | Specific `decode_error` alert mapping is not proven. |
| R004-045b | §4.4.2.2, §4.4.2.4 | Certificate-policy failures must produce appropriate certificate-related alerts such as `unsupported_certificate` or `bad_certificate`. | GAP | Certificate policy tests return typed errors. | End-to-end mapping from policy errors to RFC alert descriptions is not proven. |
| R004-046 | §4.4.3 | CertificateVerify must appear after Certificate and before Finished. | PARTIAL | `src/ClientHandshake.zig` test `processFlight: rejects Finished before EncryptedExtensions`; valid full-flight tests. | Add targeted CV-before-Certificate and Finished-before-CV tests if not already covered by broader malformed-flight rejection. |
| R004-047 | §4.4.3 | CertificateVerify signature must use a scheme offered in the client's `signature_algorithms`, be compatible with the certificate key, use RSA-PSS for RSA regardless of PKCS#1 certificate-signature support, exclude SHA-1, and verify over `0x20*64 || context || 0x00 || transcript_hash`. | GAP | `src/certificate.zig` tests `verifySignature: valid ECDSA P-256 signature`, `verifySignature: wrong transcript hash`, `verifySignature: server signature is not valid for client context`, `verifySignature: wrong handshake type`; `src/ClientHandshake.zig` test `processFlight: rejects wrong CertificateVerify signature`. | Core transcript/signature verification is covered, but ztls verifies against global supported schemes rather than the client's actual offered list; PKCS#1-v1_5 CertificateVerify rejection and key-mismatch tests are missing. |
| R004-048 | §4.4.4 | Finished verify_data and finished_key must be computed from transcript hash with HKDF/HMAC; recipients must verify and abort on failure. | PROVEN | `src/finished.zig` tests `verify: wrong verify_data`, `encode then verify round-trip`; `src/ClientHandshake.zig` tests `clientFinished: RFC 8448 §3 emits Finished and upgrades to app keys`, `processFlight: rejects wrong server Finished verify_data`; `src/ServerHandshake.zig` test `processClientFinished: verifies Finished and installs app keys`. | Alert-description mapping for decrypt_error is not exhaustively proven. |
| R004-049 | §4.4.4 | Records following Finished must use application traffic keys. | PROVEN | `src/ClientHandshake.zig` tests `clientFinished: RFC 8448 §3 emits Finished and upgrades to app keys`, `handleRecord: application data returns plaintext and resets the flood counter`; `src/ServerHandshake.zig` tests `processClientFinished: verifies Finished and installs app keys`, `application data: server sends and receives`. | |
| R004-050 | §4.6.1 | NewSessionTicket may be sent after client Finished; clients must ignore unknown extensions; `early_data` extension format must be uint32. | PROVEN | `src/NewSessionTicket.zig` tests `parse: valid NewSessionTicket`, `parse: captures early_data max_early_data_size`, `parse: skips unknown ticket extension`, `parse: rejects duplicate early_data extension`, `parse: rejects malformed early_data length`; `src/ClientHandshake.zig` tests `handleRecord: NewSessionTicket is parsed and ignored`, `handleRecord: malformed NewSessionTicket is rejected`. | Server emission/resumption semantics are #2. |
| R004-051 | §4.6.1 | Server NewSessionTicket `ticket_lifetime` must not exceed seven days; server ticket_age_add and nonce must be fresh/unique; clients must not cache tickets longer than seven days. | PARTIAL | ztls parses and discards tickets. | Client-side no-cache behavior trivially satisfies the cache lifetime rule. Server ticket emission constraints belong to resumption/ticket support (#2). |
| R004-052 | §4.6.3, §7.2 | KeyUpdate may be sent only after Finished; sender must use old keys for the KeyUpdate record then next-generation keys; receiver must ratchet receive keys; `update_requested` response must be sent before next Application Data; invalid request values abort. | PARTIAL | `src/ClientHandshake.zig` tests `handleRecord: server KeyUpdate(update_requested) ratchets rx and responds`, `handleRecord: KeyUpdate not at record boundary is rejected`, `handleRecord: KeyUpdate flood is rejected`; `src/ServerHandshake.zig` tests `handleRecord: client KeyUpdate(update_requested) ratchets rx and responds`, `handleRecord: client KeyUpdate(update_not_requested) ratchets rx only`, `sendKeyUpdate: server-initiated KeyUpdate encrypts under old key then ratchets tx`, `key update: simultaneous update_requested remains connected`; `src/handshake.zig` test `parseKeyUpdate`. | Connected-state ratcheting is well covered. Direct KeyUpdate-before-Finished and invalid connected-state request-byte tests are missing/partial. |
| R005-001 | §5 | Unencrypted ChangeCipherSpec compatibility records of exactly byte `0x01` may be received after first ClientHello and before peer Finished and must be silently dropped. | GAP | `src/ServerHandshake.zig` test `handleRecord: drops ChangeCipherSpec while waiting for ClientHello`; client full RFC 8448 path handles CCS. | ztls drops CCS by content type without proving the payload is exactly `0x01` or the allowed window. |
| R005-002 | §5 | Any other CCS value, protected CCS, or CCS outside the compatibility window must be treated as unexpected. | GAP | Connected inner content-type rejection: `src/ClientHandshake.zig` test `handleRecord: post-handshake unexpected inner content type is rejected`; `src/ServerHandshake.zig` test `handleRecord: illegal post-handshake inner content type is rejected`. | Protected CCS is rejected through encrypted inner-type checks, but plaintext CCS value/window validation is incomplete/untested. |
| R005-003 | §5 | Implementations must not send undefined record types; unexpected record types must terminate with `unexpected_message`. | PROVEN | Record emitters only use handshake, application_data, and alert records; tests reject application_data during handshake and illegal post-handshake inner types. | ztls receives/drops CCS for middlebox compatibility but does not emit CCS in production paths. Alert mapping should remain in `NEGATIVE_SPACE.md`. |
| R005-004 | §5.1 | Handshake messages must not be interleaved with other record types and must not span key changes; ClientHello, EndOfEarlyData, ServerHello, Finished, KeyUpdate must align to record boundary. | PARTIAL | KeyUpdate boundary tests on both sides; handshake reassembly tests `processFlight: reassembles handshake message split across records`, `processFlight: handshake message spanning records needs buffer`. | Send-side alignment is structural. Receive-side key-change alignment is directly tested for KeyUpdate but not every listed key-change message. EndOfEarlyData is #3. |
| R005-005 | §5.1 | Handshake records must not carry zero-length fragments; Alert records must not be fragmented or coalesced. | PARTIAL | `src/alert.zig` encode/parse tests; `sendAlert` tests on both client/server. | Structural for sends; receive-side zero-length handshake/alert inner tests are missing. |
| R005-006 | §5.1 | TLS 1.3 records use legacy_record_version 0x0303 except initial ClientHello may use 0x0301; recipients ignore legacy_record_version. | PARTIAL | `src/frame.zig` tests `parseHeader: valid plaintext record`, `Header.init round-trips with parseHeader`; `src/client_hello.zig` test `encode: handshake type and legacy version`. | `frame.parseHeader` does not branch on legacy version, but there is no dedicated odd-version acceptance test. |
| R005-007 | §5.1, §5.2 | TLSPlaintext length must not exceed 2^14; TLSCiphertext length must not exceed 2^14+256; oversize means `record_overflow`. | PROVEN | `src/frame.zig` tests `parseHeader: max valid length`, `parseHeader: length exceeds max`; `src/RecordBuffer.zig` test `next: oversized length is rejected`; `src/RecordLayer.zig` tests `encrypt/decrypt: maximum plaintext fragment length`, `encrypt: buffer too short`; tlsfuzzer oversized-record coverage. | Error-to-alert mapping should be audited separately. |
| R005-008 | §5.1 | Application Data records must not be written unprotected. | PROVEN | `sendApplicationData` only operates connected/encrypted; app-data tests decrypt through `RecordLayer`. | API shape prevents plaintext app-data emit. |
| R005-009 | §5.2 | TLSCiphertext outer type is always application_data; non-23 outer type must be rejected for protected records. | PROVEN | `src/RecordLayer.zig` test `decrypt: wrong content type`; encrypted post-handshake unexpected-type tests on both handshakes. | |
| R005-010 | §5.2 | AEAD failure must terminate with `bad_record_mac`; AAD is the five-byte TLSCiphertext header; AEAD expansion must be <=255. | PROVEN | `src/aead.zig` tamper tests for ciphertext/tag/AAD; `src/ClientHandshake.zig` tests `handleRecord: corrupted encrypted flight is rejected`; `src/RecordLayer.zig` decrypt tests; AEAD wrappers use 16-byte tags. | Exact alert emission is tested on selected paths, not every caller path. |
| R005-011 | §5.2, §5.4 | TLSInnerPlaintext content type is last non-zero byte; padding bytes are zero; all-zero inner plaintext must abort with `unexpected_message`; scan only decrypted plaintext. | PARTIAL | `src/memx.zig` `lastIndexOfNonZero` tests; `src/RecordLayer.zig` fuzz `decrypt handles arbitrary input`. | Add direct encrypted all-zero/zero-length handshake-alert tests. ztls sends minimal padding only. |
| R005-012 | §5.3 | Record sequence number starts at zero for each traffic key, first record uses sequence zero, nonce is padded seq XOR write_iv, and sequence wrap must rekey or terminate. | PROVEN | `src/aead.zig` nonce construction tests; `src/RecordLayer.zig` tests `encrypt/decrypt: sequence numbers advance`, `encrypt: sequence number overflow`, `decrypt: sequence number overflow`, `encrypt/decrypt: key update required at AEAD usage limit`. | |
| R005-013 | §5.5 | Implementations should update keys before AEAD usage limits. | PROVEN | `src/aead.zig` exposes `keyUsageLimit`; `src/RecordLayer.zig` test `encrypt/decrypt: key update required at AEAD usage limit`. | SHOULD, not MUST. |
| R006-001 | §6 | Alerts are encrypted according to current connection state. | PROVEN | `src/ClientHandshake.zig` tests `sendAlert: plaintext fatal alert before ServerHello`, `sendAlert: encrypted close_notify after handshake`; `src/ServerHandshake.zig` tests `sendAlert: plaintext fatal alert before ClientHello`, `sendAlert: encrypted close_notify after handshake`. | |
| R006-002 | §6, §6.2 | Receiving/transmitting fatal error alerts must close the connection; no further data may be sent/received after fatal error. | PARTIAL | `src/ClientHandshake.zig` test `handleRecord: fatal alert returns PeerAlert`; server/client alert send tests. | Sans-I/O caller must stop driving after fatal errors. Engine does not yet carry a terminal-dead state. |
| R006-003 | §6 | Failed connection secrets and keys must be forgotten. | PARTIAL | `src/RecordLayer.zig` test `deinit: clears caller-visible traffic key material`; `ClientHandshake.deinit`/`ServerHandshake.deinit` zero suites/keypairs. | Caller must call `deinit`; intermediate-secret zeroization is a gap/SHOULD. |
| R006-004 | §6 | Alerts in §6.2 must be sent fatal and treated as error alerts regardless of AlertLevel; unknown alerts must be treated as error alerts. | PROVEN | `src/alert.zig` tests `parse: warning-level error alert is fatal`, `parse: unknown alert description is fatal`, and `parse: user_canceled is not fatal`. | Dispatch paths already treat every non-`close_notify` alert as `PeerAlert`; the helper now matches TLS 1.3's implicit severity rules. |
| R006-005 | §6 | Decode failures must produce `decode_error`; semantic invalidity should produce `illegal_parameter`. | PARTIAL | Parser tests across frame/handshake/server_hello/client_hello; `src/handshake.zig` test `parseKeyUpdate`. | End-to-end alert-description mapping is not complete. |
| R006-006 | §6.1 | close_notify must be sent before closing write side; data after close_notify must be ignored; close_notify-before-EOF requirements belong to applications with post-TLS data. | PARTIAL | `src/ClientHandshake.zig` test `handleRecord: close_notify returns closed`; close_notify send tests on both roles; `examples/tcp_loopback.zig` demonstrates clean shutdown. | Engine does not enforce ignore-after-close state. EOF/application semantics are caller-owned. |
| R007-001 | §7.1 | TLS 1.3 HKDF key schedule, HKDF-Expand-Label, Derive-Secret, zero PSK early secret, handshake secret, master secret, traffic secrets, and finished keys must match RFC construction. | PROVEN | `src/hkdf.zig` RFC 8448 tests `HkdfSha256.earlySecret`, `handshakeSecret`, `masterSecret`, `clientHandshakeTrafficSecret`, `serverHandshakeTrafficSecret`, `finishedKey`; `src/ClientHandshake.zig` test `deriveHandshakeKeys: RFC 8448 §3 server handshake key/iv/finished`. | PSK binder labels are #2. |
| R007-002 | §7.1, §7.2 | Secrets and old traffic keys should be erased after use/key update. | PARTIAL | `deinit` zeroization paths and `RecordLayer` deinit test. | Intermediate secrets and replaced traffic keys are not explicitly wiped immediately after derivation/ratchet. SHOULD-level gap. |
| R007-003 | §7.2 | KeyUpdate traffic secret update is `HKDF-Expand-Label(secret, "traffic upd", "", Hash.length)`. | PROVEN | `src/hkdf.zig` tests `HkdfSha256.nextTrafficSecret: RFC 8446 §7.2`, `HkdfSha384: traffic key/iv, finished key, traffic-upd`; handshake KeyUpdate tests. | |
| R007-004 | §7.3 | Traffic write keys and IVs derive via `HKDF-Expand-Label(secret, "key"/"iv", "", length)`. | PROVEN | `src/hkdf.zig` tests `HkdfSha256.trafficKey: RFC 8448 §3 server handshake`, `HkdfSha256.trafficIv: RFC 8448 §3 server handshake`; `src/ClientHandshake.zig` key install tests. | |
| R007-005 | §7.4 | ECDHE shared secret handling for X25519 must use raw shared secret and abort all-zero output. | PROVEN | `src/x25519.zig` test `sharedSecret: RFC 7748 X25519 vector`; Wycheproof low-order tests. | FFDHE and NIST curves are not supported yet (#6/#22). |
| R007-006 | §7.5 | Exporters must use `exporter_master_secret`; early exporter should have a separate interface. | OUT-OF-SCOPE | No exporter API; not part of current readiness surface. | Track separately if exporter support becomes a supported feature. |
| R009-001 | §9.1 | Must implement `TLS_AES_128_GCM_SHA256`; should implement `TLS_AES_256_GCM_SHA384` and `TLS_CHACHA20_POLY1305_SHA256`. | PROVEN | `src/aead.zig` tests for all three AEADs; `src/ServerHandshake.zig` test `in-memory authenticated client-server handshake suite matrix`; OpenSSL interop tests. | |
| R009-002 | §9.1 | Must support `rsa_pkcs1_sha256` for certificate signatures, `rsa_pss_rsae_sha256` for CertificateVerify and certificates, `ecdsa_secp256r1_sha256`, and P-256 key exchange; should support X25519. | GAP | `signature_scheme.zig` supports RSA-PSS and ECDSA P-256 for CertificateVerify; certificate policy recognizes supported certificate signature algorithms; `kex.zig`/X25519 implementation supports X25519. | Certificate-signature MTI coverage needs explicit tests, and P-256 key exchange is not implemented/proven (#6/#22). |
| R009-003 | §9.2 | MTI extensions: `supported_versions`, `cookie`, `signature_algorithms`, `signature_algorithms_cert`, `supported_groups`, `key_share`, and `server_name` where applicable. | PARTIAL | Implemented/tested: `supported_versions`, `signature_algorithms`, `supported_groups`, `key_share`, `server_name`; SNI tests in `ServerHandshake.zig`; ClientHello/parser tests. | `cookie` live support is tied to HRR (#1). Sending `signature_algorithms_cert` can be omitted when policy matches `signature_algorithms`, but receiving and honoring a distinct peer `signature_algorithms_cert` policy is missing/unproven. |
| R009-004 | §9.2 | ClientHello advertising TLS 1.3 must contain required extensions for certificate-authenticated ECDHE; server receiving nonconforming ClientHello must abort with `missing_extension`. | PARTIAL | Client encode path includes required extensions; `client_hello.zig` malformed parse tests. | Missing-extension alert mapping and individual missing-extension tests need expansion. |
| R009-005 | §9.3 | Endpoint must support advertised parameters and ignore unrecognized cipher suites/extensions/parameters as specified. | PARTIAL | ztls advertises only implemented suites/groups; unknown extension tests exist; cipher-suite all-unsupported rejection exists. | Mixed unknown/known cipher-suite behavior and some unrecognized extension blocks need tests. |
| R009-006 | §9.3 | Middlebox-specific forwarding invariants for compliant TLS middleboxes. | N/A | ztls is not a TLS middlebox. | §9.3 endpoint extension-negotiation invariants are covered above by §4.2 and R009-005. |

## Current high-value gaps exposed by the matrix

These are not the only gaps above, but they are the ones most likely to matter
for the next correctness slice:

- ServerHello downgrade-sentinel check is not explicitly implemented/tested.
- ClientHello parser/server path needs direct tests for missing
  `supported_versions` and mixed unknown/known cipher-suite lists.
- Server Certificate with non-empty `certificate_request_context`, empty
  certificate_list alert mapping, CertificateEntry extension validation, and
  intermediate signature-algorithm policy need clearer decisions/tests.
- CertificateVerify scheme must be cross-checked against what the client offered,
  not only against ztls's global supported schemes.
- CCS compatibility handling should validate the payload byte and allowed window.
- `close_notify` should transition the engine into an ignore/reject-after-close
  state or the caller-boundary contract should explicitly say this is outside
  the core engine.
- Immediate zeroization of intermediate secrets and old traffic keys after
  KeyUpdate is not proven.
- MTI requirements not met or not proven: `rsa_pkcs1_sha256` for certificate
  signatures, P-256 key exchange, live cookie/HRR support, and
  `signature_algorithms_cert`.

## Out-of-scope feature gates

The following RFC requirement families are intentionally not counted as current
supported-surface failures, but they remain mandatory for any future claim that
ztls implements the corresponding TLS 1.3 feature:

| Feature | RFC areas | Tracking issue |
|---|---|---|
| HelloRetryRequest retry state machine, transcript collapse, cookie echo | §2.1, §4.1.4, §4.2.2, §4.2.8 | #1 |
| PSK/session resumption, binders, ticket storage, PSK modes | §2.2, §4.2.9, §4.2.11, §4.6.1 | #2 |
| 0-RTT, EndOfEarlyData, anti-replay | §2.3, §4.2.10, §4.5, §8 | #3 |
| Client certificate authentication and post-handshake authentication | §4.2.4–§4.2.6, §4.3.2, §4.4.2.3, §4.6.2 | #4 |
| Provider-backed P-256/P-384/PQ named groups | §4.2.8, §7.4, §9.1 | #6 / #22 |
| Exporters | §7.5 | untracked until needed |
